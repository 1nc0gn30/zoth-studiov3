interface:
  display_name: "Hosted Agent Registry"
  short_description: "Build public registries that agents can fetch."
  default_prompt: "Create a hosted registry that AI agents can fetch and use to load parameterized skills."
