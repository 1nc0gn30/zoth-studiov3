{
  "schema_version": "agent-pack/v1",
  "id": "example-pack",
  "name": "Example Agent Pack",
  "version": "1.0.0",
  "summary": "Turns an agent into a focused specialist for one job.",
  "compatibility": ["generic-agent"],
  "parameters": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "mode": {"type": "string", "enum": ["fast", "balanced", "deep"]}
    },
    "required": ["mode"]
  },
  "instructions": [
    {"type": "markdown", "url": "https://example.com/packs/example/1.0.0/SKILL.md", "checksum_sha256": ""}
  ],
  "resources": [],
  "trust": {
    "publisher": "",
    "license": "",
    "permissions": ["read_workspace"],
    "requires_user_approval": ["network", "shell", "deploy"],
    "update_policy": "immutable-versioned-releases"
  }
}
