interface:
  display_name: "Agent Fetch Client"
  short_description: "Implement clients that fetch hosted agent packs."
  default_prompt: "Build a CLI or integration that fetches hosted agent skill packs and validates them."
