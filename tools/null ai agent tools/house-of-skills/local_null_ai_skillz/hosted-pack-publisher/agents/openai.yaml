interface:
  display_name: "Hosted Pack Publisher"
  short_description: "Publish versioned hosted skill packs."
  default_prompt: "Prepare a versioned hosted skill pack release with manifests, checksums, and docs."
