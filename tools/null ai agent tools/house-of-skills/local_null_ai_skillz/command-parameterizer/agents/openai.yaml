interface:
  display_name: "Command Parameterizer"
  short_description: "Turn user commands into safe agent parameters."
  default_prompt: "Create a parameter schema and command mapping for transforming an agent behavior."
