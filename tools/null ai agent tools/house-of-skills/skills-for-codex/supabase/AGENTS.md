## Supabase local rules
- Treat schema and RLS as first-class concerns
- Call out required migrations explicitly
- Never assume permissive policies
- Mention if a change affects auth, profile lookup, or report access patterns
- Prefer incremental migrations over destructive rewrites
