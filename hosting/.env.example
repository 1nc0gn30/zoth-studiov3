# Copy to .env and fill in (never commit .env)
#
# Cloudflare Zero Trust → Networks → Tunnels → Create tunnel
# → Docker install → copy the token after "--token"

CLOUDFLARE_TUNNEL_TOKEN=eyJhIjoiREPLACE_ME
